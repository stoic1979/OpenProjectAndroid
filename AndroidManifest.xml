<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wbapp.openproject"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="12"
        android:targetSdkVersion="20" />

    <!-- ++++++++++++++++++revmob begin++++++++++++++++++ -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- ++++++++++++++++++revmob end++++++++++++++++++++ -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo.Light" >
        
          <!-- ++++++++++++++++++revmob begin++++++++++++++++++ -->
        <activity
          android:name="com.revmob.FullscreenActivity"
            android:configChanges="keyboardHidden|orientation"
            android:theme="@android:style/Theme.Translucent" >
        </activity>

        <meta-data
            android:name="com.revmob.app.id"
            android:value="56c47869b8ab7a6a1ef85bd5" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- ++++++++++++++++++revmob end++++++++++++++++++++ -->
        <activity
            android:name="com.wbapp.openproject.Splash"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.wbapp.openproject.MainActivity"></activity>
        <activity android:name="com.wbapp.openproject.MainTabActivity"></activity>
    </application>

</manifest>
